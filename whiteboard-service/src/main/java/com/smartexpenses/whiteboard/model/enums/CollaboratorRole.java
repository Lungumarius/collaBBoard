package com.smartexpenses.whiteboard.model.enums;

public enum CollaboratorRole {
    OWNER,   // Full control, can delete board
    EDITOR,  // Can create, edit, delete shapes
    VIEWER   // Read-only access
}
